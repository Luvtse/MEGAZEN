import type {NextFunction,Request,Response} from "express"; declare global{namespace Express{interface Request{tenantId:string}}} export const tenantMiddleware=(req:Request,res:Response,next:NextFunction)=>{const id=req.header("x-tenant-id");if(!id){res.status(400).json({success:false,data:null,error:{code:"TENANT_REQUIRED",message:"x-tenant-id header is required"},timestamp:new Date().toISOString()});return}req.tenantId=id;next()};

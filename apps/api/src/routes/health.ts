import {Router} from "express"; import {prisma} from "../lib/prisma.js"; import {redis} from "../lib/redis.js"; import {ok} from "../lib/response.js"; export const healthRouter=Router(); healthRouter.get("/",async(_req,res,next)=>{try{await prisma.$queryRaw`SELECT 1`;let status="unavailable";try{if(redis.status==="wait")await redis.connect();status=await redis.ping()}catch{}ok(res,{service:"megazen-api",status:"healthy",database:"connected",redis:status,uptimeSeconds:Math.round(process.uptime())})}catch(e){next(e)}});
